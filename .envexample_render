# Render deployment environment variables
#
# IMPORTANT: Render automatically provides RENDER_EXTERNAL_URL
# You do NOT need to manually set APP_BASE_URL or VITE_API_BASE_URL!
# The application will automatically use RENDER_EXTERNAL_URL during deployment.
#
# To use a custom domain instead of Render's auto-generated URL,
# uncomment and set APP_BASE_URL below:
# APP_BASE_URL=https://your-custom-domain.com

APP_SECRET_TOKEN=change-me-secret
TARGET_EMAIL=demo@example.com
REPORT_LANGUAGE=en
STORE_TRANSCRIPTS=true

# Email provider configuration
# IMPORTANT: Render.com blocks SMTP ports (25, 465, 587) to prevent spam.
# Use SendGrid for reliable email delivery on Render.
EMAIL_PROVIDER=smtp
EMAIL_DEFAULT_SENDER=your-verified-email@example.com

# SendGrid configuration (REQUIRED for Render deployment)
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# Make sure to verify your sender email in SendGrid dashboard
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here

# SMTP configuration (optional, won't work on Render due to port blocking)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=demo@example.com
# SMTP_PASSWORD=change-me

# GPT-5 API Configuration (required for LLM-based evaluations)
GPT5_API_KEY=your-gpt5-api-key-here
GPT5_API_BASE_URL=https://api.openai.com/v1
GPT5_MODEL=gpt-4
GPT5_TEMPERATURE=0.3

# Frontend build variables
# Note: VITE_API_BASE_URL is automatically set during build from RENDER_EXTERNAL_URL
# You only need to set VITE_APP_SECRET_TOKEN (must match APP_SECRET_TOKEN above)
VITE_APP_SECRET_TOKEN=change-me-secret
