VITE_API_BASE_URL=http://localhost:8000
# IMPORTANT: This must match the APP_SECRET_TOKEN set in the backend .env file
# Generate a secure token (minimum 32 characters) using:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or: openssl rand -base64 32
VITE_APP_SECRET_TOKEN=example-change-this-to-a-secure-random-token-minimum-32-chars
